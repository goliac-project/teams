admin_team: admin

enable_rulesets: true
rulesets:
  - pattern: .*
    ruleset: default

max_changesets: 50

destructive_operations:
  repositories: true
  teams: true
  users: true
  rulesets: true

usersync:
  plugin: noop
