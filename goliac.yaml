admin_team: admin

enable_rulesets: true
rulesets:
  - pattern: .*
    ruleset: default

max_changesets: 50

destructive_operations:
  repositories: true
  teams: false
  users: false
  rulesets: false

usersync:
  plugin: noop
